import styled from 'styled-components';

export const Container = styled.div`
  width: 100%;

  display: flex;

  & .time {
    margin-bottom: 0.5rem;
    font-weight: bold;
  }

  & .repair_list {
    margin-bottom: 0.5rem;

    text-align: start;
  }
`;
